# surrogate-model-for-droplet-spreading
In this project, we build a surrogate model for droplet spreading. The underlying mechanism is based on the volume fraction based lattice boltzmann method. However, we will not use full lattice boltzmann, but the spreading rate at each grid point will be obtained by CFD model or experiment through ML method.
